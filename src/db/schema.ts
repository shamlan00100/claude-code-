// Single entry point for the database schema. Auth tables are generated by
// Better Auth (auth-schema.ts); the product's tables live in domain-schema.ts.
export * from './auth-schema'
export * from './domain-schema'
